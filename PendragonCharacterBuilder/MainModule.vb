Module MainModule

    Sub Main()
        'The basic program will ask for a name and gender, names for father and grandfather,
        'and assume that you're a Salisbury knight from the core book.
        'It won't assign your attributes or your various bonuses, just the standard stuff.
        'Then it'll output an XML file, then convert that into basic forum bbcode and plain text.

        'At this point I'm just dicking around trying to sort branches and wotnot.
    End Sub

End Module
